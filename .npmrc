# BuildCore is deployed as its own repo (Vercel). Keep package-lock.json committed here.
# When working inside the zenformed-workspace folder locally, prefer:
#   npm run install:standalone
package-lock=true
