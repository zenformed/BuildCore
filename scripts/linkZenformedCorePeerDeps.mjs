/**
 * @deprecated Install from the ZenformedCore monorepo root instead:
 *   cd .. && npm install
 *
 * See ForgeCore/scripts/linkZenformedCorePeerDeps.mjs.
 */
console.log(
  '[linkZenformedCorePeerDeps] no-op — use `npm install` at the ZenformedCore repo root (workspaces).'
);
